{
  "framework": "nextjs",
  "installCommand": "(command -v bun >/dev/null 2>&1 && bun install --frozen-lockfile) || (npm ci --no-audit --no-fund || npm install --no-audit --no-fund)",
  "buildCommand": "(command -v bun >/dev/null 2>&1 && bun run build) || npm run build"
}
